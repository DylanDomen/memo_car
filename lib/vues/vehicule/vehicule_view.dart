import 'package:flutter/material.dart';

class VehiculeView extends StatelessWidget {
  const VehiculeView({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Container(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
